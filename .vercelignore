# Excluded from the upload to Vercel. Note this also removes files from the
# BUILD, not just the runtime bundle, so anything the build touches must stay.
# web/scripts is required by web's build script (spa-fallback) - do not add it.
Dockerfile
docker-compose.yml
render.yaml
.github
**/dev-dist
